# Migrations

SQL migrations for the self-hosted Supabase (supabase.arrowair.com), numbered and applied
in order. First real migration lands with the initial schema design.

The legacy schema (for data import) lives in
[`project-flight-tracking/supabase`](https://github.com/Arrow-air/project-flight-tracking/tree/main/supabase).
